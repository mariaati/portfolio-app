function Footer() {
  return (
    <footer style={styles.footer}>
      
    </footer>
  );
}

const styles = {
  footer: {
    background: '#eeeeee',
    padding: '1rem',
    textAlign: 'center',
    marginTop: '2rem'
  }
};

export default Footer;
